Timepoints:
< Enter timepoints separated by tabs, on single line >

Parameters:
< Set parameter values (constants in rate equations).  These must be in the
order they're listed in the MEA output file (i.e. c[0],c[1],c[2] etc).  If
multiple random restarts are used, set the range used for each parameter
by writing the upper values on the first line, and the lower values on the
second >

Set parameter limits:
< For constrained optimization, set the upper and lower limits for allowed
parameter values using the first and second line respectively.  If no
limit for a particular parameter, use 'N' to indicate this. >

Initial conditions:
< Set initial conditions (values of each species at the first time point).
Give values up to the highest moment you want to specify, any beyond this
will be set to 0.  Specify in the order given in the MEA output file.
To use random restarts, set the upper and lower limit of the range for each
condition >

Set initial conditions limits:
< For constrained optimization, set the upper and lower limits allowed
for each value.  Use 'N' if no limit >
	
Fixed(0)/variable(1) parameters:
< Specify whether a given parameter value should be optimised (1) or fixed (0)>

Fixed(0)/variable(1) initial conditions:
< Specify whether an initial condition should be optimised (1) or fixed (0) >
